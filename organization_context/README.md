# Organization Context

This folder is for company-specific guidance inputs that should shape semantic synthesis, contract generation, and governance behavior.

Recommended files:

- `business_glossary.json`
- `governance_rules.json`
- `org_context.json`

These files are optional, but when present they should be treated as first-class behavior inputs into the semantic layer.
