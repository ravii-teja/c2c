# Benchmarks

Benchmark definitions for semantic quality, insight quality, drift, and governance tests belong here.

Current starter asset:

- `semantic_sales_v1.json` as a minimal gold benchmark for semantic contract scoring across entities, metrics, relationships, and governance tags.
